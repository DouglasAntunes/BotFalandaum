"use strict";

class SoundCollection {
    constructor(prefix, commands, sounds) {
        this.prefix = prefix;
        this.commands = commands;
        this.sounds = sounds;
    }
 
    load() {

    }

    random() {
        
    }

    randomRange(min, max) {

    }
};